# Núcleo — dependências compartilhadas por todos os entrypoints (CLI e pipeline).
# Deps de domínio ficam nos arquivos específicos (web, ia, folha, conflito);
# deps de teste ficam em requirements-dev.txt.
requests>=2.31.0
python-dotenv>=1.0.0
APScheduler>=3.10.0
